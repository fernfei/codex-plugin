<idea-plugin>
    <id>com.codex.copy-path</id>
    <name>Codex</name>
    <vendor>Codex</vendor>

    <description><![CDATA[
        <p><strong>Codex</strong> - Copy the project-relative file path from the editor context menu and add line numbers when text is selected.</p>
        <ul>
            <li>Select code and use <kbd>Ctrl+Alt+K</kbd> (Win/Linux) or <kbd>Cmd+Alt+K</kbd> (Mac)</li>
            <li>Copies <code>@ProjectName/path</code>; adds <code>#L</code> range when selected</li>
        </ul>

        <hr/>

        <p><strong>Codex</strong> - 在编辑器右键菜单中复制项目内文件路径，选中时附加行号。</p>
        <ul>
            <li>选中代码后使用 <kbd>Ctrl+Alt+K</kbd>（Win/Linux）或 <kbd>Cmd+Alt+K</kbd>（Mac）</li>
            <li>复制 <code>@项目名/路径</code>，选中时追加 <code>#L</code> 行号</li>
        </ul>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <actions>
        <!-- 复制项目路径与行号到剪贴板 -->
        <action id="Codex.CopyProjectPathAction"
                class="com.codex.SendSelectionToTerminalAction">
            <!-- 添加到编辑器右键菜单（显示在最上面） -->
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt K"/>
            <keyboard-shortcut keymap="Mac OS X" first-keystroke="meta alt K"/>
            <keyboard-shortcut keymap="macOS" first-keystroke="meta alt K"/>
        </action>
    </actions>
</idea-plugin>
